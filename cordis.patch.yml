# dsh-opencodego-usage-plugin bundle patch: activating the plugin row when
# this package joins the profile bundle layer (dsh plugin add).
- insert:
    - id: ui-opencode-usage
      name: '@deepseek-ai/dsh-client-ui-opencode-usage'
      config:
        apiKeyEnv: OPENCODE_GO_API_KEY
